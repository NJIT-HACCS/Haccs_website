import { Heading } from "@chakra-ui/react";
import React from "react";

const ErrorPage = () => {
    return (
        <Heading >
            Invalid Page
        </Heading>
    )
};

export default React.memo(ErrorPage);